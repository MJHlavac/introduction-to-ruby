# Use Array#each to iterate over colors and print each element.

colors = ['red', 'yellow', 'purple', 'green']

colors.each {|color| puts "I am the color #{color}!"} # each method iterates over each color plus string interpolation. 

# g shit