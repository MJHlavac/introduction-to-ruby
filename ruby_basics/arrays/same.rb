# Compare array1 and array2 and print true or false based on whether they match.

array1 = [1, 5, 9]
array2 = [1, 9, 5]

p array1 == array2 # False because the order of arrays matters when comparing them. == comparator will return 